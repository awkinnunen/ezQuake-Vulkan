diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -477,7 +477,7 @@
         add_custom_command(
                 OUTPUT ${output_file}
                 COMMAND ${GLSLANG_VALIDATOR} -V ${source_file} -o ${output_file}
-                DEPENDS ${source_file} ${VULKAN_SHADER_DIR}/vk_world_push.glsl ${VULKAN_SHADER_DIR}/vk_world_flags.h
+                DEPENDS ${source_file} ${VULKAN_SHADER_DIR}/vk_world_push.glsl ${VULKAN_SHADER_DIR}/vk_world_flags.h ${VULKAN_SHADER_DIR}/vk_skybox_uv.glsl
                 COMMENT "Compiling Vulkan shader ${source_file}"
                 VERBATIM
         )
diff --git a/src/vulkan_shaders/vk_skybox_uv.glsl b/src/vulkan_shaders/vk_skybox_uv.glsl
new file mode 100644
--- /dev/null
+++ b/src/vulkan_shaders/vk_skybox_uv.glsl
@@ -0,0 +1,8 @@
+// OpenAI Codex, 2026-09-13. GPL-2.0-or-later.
+vec2 skyboxClampUv(vec2 uv, ivec2 faceSize)
+{
+	// Preserve the original one-texel inset for 512x512 faces, per axis.
+	// A 1- or 2-texel axis collapses to its centre, never inverted bounds.
+	vec2 inset = min(vec2(0.5), 1.0 / vec2(max(faceSize, ivec2(1))));
+	return clamp(uv, inset, vec2(1.0) - inset);
+}
diff --git a/src/vulkan_shaders/vk_world_flat.frag b/src/vulkan_shaders/vk_world_flat.frag
--- a/src/vulkan_shaders/vk_world_flat.frag
+++ b/src/vulkan_shaders/vk_world_flat.frag
@@ -16,6 +16,7 @@
 layout(set = 1, binding = 0) uniform sampler2D lightmapTexture[2];
 
 #include "vk_world_push.glsl"
+#include "vk_skybox_uv.glsl"
 
 layout(location = 0) out vec4 fragColour;
 
@@ -30,6 +31,16 @@
 		return dir.y < 0.0 ? 3 : 2;
 	}
 	return dir.z < 0.0 ? 5 : 4;
+}
+
+ivec2 skyboxFaceSize(int axis)
+{
+	if (axis == 0) return textureSize(skyboxFace0, 0);
+	if (axis == 1) return textureSize(skyboxFace1, 0);
+	if (axis == 2) return textureSize(skyboxFace2, 0);
+	if (axis == 3) return textureSize(skyboxFace3, 0);
+	if (axis == 4) return textureSize(skyboxFace4, 0);
+	return textureSize(skyboxFace5, 0);
 }
 
 vec2 skyboxUv(int axis, vec3 dir)
@@ -69,7 +80,7 @@
 		t = dir.x / dv;
 	}
 
-	vec2 uv = clamp((vec2(s, t) + vec2(1.0)) * 0.5, vec2(1.0 / 512.0), vec2(511.0 / 512.0));
+	vec2 uv = skyboxClampUv((vec2(s, t) + vec2(1.0)) * 0.5, skyboxFaceSize(axis));
 	uv.y = 1.0 - uv.y;
 	return uv;
 }
